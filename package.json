{
	"name": "wisp-server-workers",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"build": "worker-build --release",
		"deploy": "wrangler deploy",
		"dev": "wrangler dev --env dev"
	},
	"devDependencies": {
		"wrangler": "^4.18.0"
	}
}
