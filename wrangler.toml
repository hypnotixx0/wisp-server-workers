name = "wisp-server-workers"
main = "build/worker/shim.mjs"
compatibility_date = "2025-10-10"

workers_dev = true

# Add build configuration for Rust
[build]
command = "worker-build --release"

[vars]
allowed_host_regex = ".*"  # Allow all hosts for your proxy

[env.dev]
# Optional dev settings
